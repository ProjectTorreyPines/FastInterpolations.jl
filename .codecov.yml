coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 1%
    patch:
      default:
        target: 95%

ignore:
  - "ext/**/*"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true

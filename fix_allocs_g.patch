--- src/phs/phs_eval.jl
+++ src/phs/phs_eval.jl
@@ -785,7 +785,8 @@
             @inbounds for dim in 1:N; Δ = Tg(query[dim]) - nb_coords[dim]; d2 += Δ * Δ; end
             w = _phs_blend_weight(sqrt(d2), blend_a, blend_a3)
             w < eps(Tg) && continue
-            f = Tv(_phs_eval_stencil(itp, nb_idx, query, ops_val, rhs_buf, coeff_buf))
+            offsets_nb, coeff_nb, hs_nb = _phs_solve_stencil!(itp, nb_idx, rhs_buf, coeff_buf)
+            f = Tv(_phs_eval_coeffs_value(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}()))
             sum_w  += w
             sum_wg += w * exp(f)
         end
@@ -821,7 +822,7 @@
                 sum_N1 += wp * dir * g + w * g * f_ξ
                 sum_W1 += wp * dir
             else
-                f = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_val))
+                f = Tv(_phs_eval_coeffs_value(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}()))
                 g = exp(f)
                 sum_w  += w
                 sum_wg += w * g
@@ -897,11 +898,11 @@
                 end
             else
                 # d≈0: weight derivatives ≈ 0
-                f    = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_val))
+                f    = Tv(_phs_eval_coeffs_value(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}()))
                 g    = exp(f)
-                f_d2 = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops))
+                f_d2 = Tv(_phs_eval_coeffs_deriv2(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ax1, ax2))
                 # For the d≈0 diagonal case, f_d1 contributes to d2g; since it's rare, compute separately
-                f_d1_sq = is_diag ? Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_d1_1))^2 : zero(Tv)
+                f_d1_sq = is_diag ? Tv(_phs_eval_coeffs_deriv1(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ax1))^2 : zero(Tv)
                 d2g = g * (f_d2 + f_d1_sq)
                 sum_w += w; sum_wg += w * g
                 sum_N2 += w * d2g
@@ -988,7 +989,7 @@
             sum_N1 += wp * dir * g + w * g * f_ξ
             sum_W1 += wp * dir
         else
-            f = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_val))
+            f = Tv(_phs_eval_coeffs_value(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}()))
             g = exp(f)
             sum_w  += w
             sum_wg += w * g
@@ -1084,21 +1085,18 @@
             end
         else
             # d≈0: weight prime/dprime ≈ 0; only stencil contributions survive.
-            f    = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_val))
+            f    = Tv(_phs_eval_coeffs_value(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}()))
             g    = exp(f)
-            f_d1 = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_d1_1))
+            f_d1 = Tv(_phs_eval_coeffs_deriv1(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ax1))
             if is_diag
-                ops_d2 = ntuple(d -> d == ax1 ? EvalDeriv2() : EvalValue(), Val(N))
-                f_d2   = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_d2))
+                f_d2   = Tv(_phs_eval_coeffs_deriv2(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ax1, ax1))
                 d2g    = g * (f_d2 + f_d1 * f_d1)
                 sum_w  += w; sum_wg += w * g
                 sum_N1 += w * g * f_d1
                 sum_N2 += w * d2g
             else
-                ops_d1_2 = ntuple(d -> d == ax2 ? EvalDeriv1() : EvalValue(), Val(N))
-                f_d1b    = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_d1_2))
-                ops_d12  = ntuple(d -> (d == ax1 || d == ax2) ? EvalDeriv1() : EvalValue(), Val(N))
-                f_d2     = Tv(_phs_eval_from_coeffs(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ops_d12))
+                f_d1b    = Tv(_phs_eval_coeffs_deriv1(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ax2))
+                f_d2     = Tv(_phs_eval_coeffs_deriv2(coeff_nb, offsets_nb, hs_nb, query, nb_coords, Val{K}(), ax1, ax2))
                 d2g      = g * (f_d2 + f_d1 * f_d1b)
                 sum_w  += w; sum_wg += w * g
                 sum_N1  += w * g * f_d1
